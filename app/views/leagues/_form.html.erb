<%= form_for(league,:html => {:class => "form-login form-wrapper form-medium mt-3", :role => "form"}) do |f| %>


  <% if league.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(league.errors.count, "error") %> prohibited this league from being saved:</h2>

      <ul>
      <% league.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <h3 class="title-divider">
      <span>Create a New League</span>
    </h3>
    <hr />
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class:"form-control", placeholder:"League Name", autofocus:"true" %>
  </div>

  <div class="form-group">
    <%= f.label :zip %>
    <%= f.text_field :zip, class:"form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :team_limit %>
    <%= f.text_field :team_limit, class:"form-control" %>
  </div>

  <div class="form-group">
      <button type="submit" class="btn btn-primary">Create</button>
  </div>
<% end %>
